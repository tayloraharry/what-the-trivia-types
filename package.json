{
  "name": "what-the-trivia-types",
  "version": "1.0.15",
  "description": "What the Trivia types and related entities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "deploy": "npm version patch && tsc && npm publish"
  },
  "dependencies": {
    "typescript": "^4.6.2"
  }
}
